apiVersion: v1
kind: Service
metadata:
  name: {{ include "threadwatch.fullname" . }}
  labels:
    {{- include "threadwatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "threadwatch.selectorLabels" . | nindent 4 }}
